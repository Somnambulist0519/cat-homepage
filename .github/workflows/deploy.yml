name: Deploy to Tencent COS

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: cos-deploy
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build
        env:
          COS_BUCKET: ${{ secrets.COS_BUCKET }}
          COS_REGION: ${{ secrets.COS_REGION }}
          SITE_BASE_URL: ${{ secrets.SITE_BASE_URL }}

      - name: Upload to Tencent COS
        uses: TencentCloud/cos-action@v1
        with:
          secret_id: ${{ secrets.COS_SECRET_ID }}
          secret_key: ${{ secrets.COS_SECRET_KEY }}
          cos_bucket: ${{ secrets.COS_BUCKET }}
          cos_region: ${{ secrets.COS_REGION }}
          local_path: ./dist
          remote_path: /
          clean: true

      # ---- CDN 刷新：ICP 备案通过后取消注释启用 ----
      # - name: Refresh CDN
      #   uses: TencentCloud/cos-action@v1
      #   with:
      #     secret_id: ${{ secrets.COS_SECRET_ID }}
      #     secret_key: ${{ secrets.COS_SECRET_KEY }}
      #     cos_bucket: ${{ secrets.COS_BUCKET }}
      #     cos_region: ${{ secrets.COS_REGION }}
      #     args: refresh -b ${{ secrets.COS_BUCKET }} -r ${{ secrets.COS_REGION }} -u "https://${{ secrets.SITE_BASE_URL }}"
